/**
 * International (expat) health insurance — annual private medical cover for
 * long-stay visa applicants.
 *
 * 2026-05-18 — provider listings removed pending affiliate-partner
 * registrations. Once partners are signed up and tracking IDs land in
 * `partnerRefValue`, the curated provider list will return. The category
 * page still renders the editorial framing (what international health
 * insurance is for, how to choose, when you don't need it).
 */
import type { RelocationService } from "@/lib/services";

export const HEALTH_INSURANCE: RelocationService[] = [];
